{
  "dependencies": {
    "express": "^4.16.4",
    "fs": "^0.0.1-security",
    "lodash.flattendeep": "^4.4.0"
  },
  "devDependencies": {
    "jest": "^24.1.0",
    "nodemon": "^1.18.10"
  },
  "scripts": {
    "start:dev": "nodemon index.js",
    "test": "jest",
    "build": "docker build -f ../Dockerfile -t paulinemh/img-explo:latest ../",
    "deploy": "npm run build && docker push paulinemh/img-explo:latest"
  }
}
